# 项目规则

- 当前产品主线是 `web/` + `server/` 的双人生活空间，生产站点为 `https://rainbow.251104.xyz`；`miniprogram/` 和 `cloudfunctions/` 仅保留为历史实现。涉及历史小程序时，先读 `README.md` 和 `miniprogram/config.js`，确认云环境后再联调。
- 客户端不直接读写数据库；数据变更通过云函数完成。新增或修改集合、字段、云函数时同步更新 README。
- 网页客户端同样不直接读写数据库；所有业务写入通过服务端 API，并同步更新 `server/README.md` 与数据库 schema。
- 删除图片、页面或依赖前，先用 `rg` 确认没有 `.wxml`、`.js` 或配置引用，并在微信开发者工具中重新编译验证。
- 云函数部署使用“云端安装依赖”；不要提交密钥、个人身份信息或未脱敏的云端数据。
