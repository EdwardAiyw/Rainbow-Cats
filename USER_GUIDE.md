# Rainbow-Cats 使用说明

Rainbow-Cats 是一个双人生活空间，支持共同维护心愿与任务、积分、礼物、菜谱、日程、账本和相册元数据。当前正式使用入口是网页版 `web/` + `server/`。

## 1. 开始使用

浏览器打开部署地址。本地默认地址为：

```text
http://127.0.0.1:3000
```

首次使用时，两位成员需要分别使用独立账号，并加入同一个双人空间。

### 创建者

1. 打开登录页，选择“创建空间”。
2. 填写账号、密码和昵称。
3. 点击“创建空间”。
4. 保存页面仅显示一次的恢复码。
5. 将页面右上角的邀请码发给另一位成员。

### 另一位成员

1. 打开同一个网址，选择“加入空间”。
2. 填写自己的账号、密码和昵称。
3. 输入创建者提供的邀请码。
4. 点击“加入空间”。
5. 保存页面仅显示一次的恢复码。

每个空间最多两位成员。请勿共用账号；需要同时测试或使用时，应打开两个独立浏览器会话，例如普通窗口和 InPrivate/无痕窗口。

## 2. 登录与账号恢复

### 登录

在登录页填写账号和密码。登录成功后，浏览器会保留当前会话，正常刷新页面不会退出。

### 忘记密码

1. 选择“恢复账号”。
2. 填写账号、创建时保存的恢复码和新密码。
3. 重置成功后返回登录页，使用新密码登录。

恢复码只在创建或加入空间时显示一次。丢失恢复码后，当前版本无法自行找回账号。

## 3. 功能说明

### 今天

“今天”页面显示当前积分、待完成心愿、近期日程和最近共同支出。右上角刷新按钮可重新读取最新数据。

### 心愿与任务

1. 进入“心愿与任务”。
2. 点击“新增任务”。
3. 填写标题、描述和积分并保存。
4. 另一位成员刷新页面后，可以点击“完成”。

任务只能完成一次。完成后，任务发布者获得对应积分；积分范围为 1 至 500。

### 礼物与收藏

1. 一位成员在“礼物架”点击“上架礼物”，填写名称、描述和所需积分。
2. 另一位成员积分足够时可以点击“兑换”。
3. 兑换后的礼物进入“我的收藏”。
4. 使用礼物后点击“使用”，系统会记录为已使用。

成员不能兑换自己上架的礼物，已兑换或已使用的记录不能重复操作。

### 菜谱

“我们的菜谱”用于保存双方共享的自建菜谱，包括菜名、简介、食材和步骤。

“官方菜谱”需要服务端配置 `TIANAPI_KEY`。未配置时，页面会显示服务尚未配置的提示，不影响自建菜谱。

### 日程

点击“新增日程”，填写标题、开始时间、结束时间和备注。双方刷新后都能看到该日程。

“连接 CalDAV”属于可选功能，需要填写有效的 CalDAV 地址、账号和应用专用密码。不要在公共设备上保存第三方日历凭据。

### 账本

点击“记一笔”，填写金额、分类、日期和备注。账本会显示本月支出与共同支出明细，金额统一保留两位小数。

### 相册

当前相册保存照片地址、说明和时间等元数据，不负责上传本地图片。添加照片时需要填写可访问的 `https://` 图片地址。

### AI 助手

AI 助手需要服务端配置 OpenClaw 网关。未配置时会显示提示，不会影响其他功能。微信助手绑定、回环接口和生产部署边界见 [OPENCLAW_WECHAT_GUIDE.md](OPENCLAW_WECHAT_GUIDE.md)。

AI 产生的写入操作会先生成待确认提案。只有成员点击“确认”后，系统才会创建任务、日程或礼物。

### 设置

设置页面可以修改当前昵称、退出登录或永久删除账号。

删除账号会永久删除当前成员资料以及相关任务、礼物、收藏和菜谱记录，无法恢复。执行前请确认不再需要这些数据。

## 4. 移动端使用

推荐使用现代版本的 Edge、Chrome、Android Chrome 或 iPhone Safari。移动端主要功能位于底部横向导航栏，可左右滑动查看全部入口。

表单弹窗内容较长时可在弹窗内上下滚动。提交前请检查输入法键盘是否遮住按钮。

## 5. 本地部署

### 环境要求

- Node.js 18 或更高版本
- PostgreSQL 14 或更高版本
- Edge 或 Chrome

### 初始化并启动

在 PowerShell 中执行：

```powershell
cd D:\program\Rainbow-Cats\server
npm install
$env:DATABASE_URL = 'postgres://postgres:数据库密码@127.0.0.1:5432/rainbow_cats'
$env:SESSION_SECRET = '请替换为随机长字符串'
$env:WEB_ORIGIN = 'http://127.0.0.1:3000'
& 'D:\Program Files\PostgreSQL\18\bin\psql.exe' `
  -v ON_ERROR_STOP=1 `
  $env:DATABASE_URL `
  -f schema.sql
npm start
```

看到以下输出表示服务已启动：

```text
Rainbow-Cats web server listening on 3000
```

健康检查地址：

```text
http://127.0.0.1:3000/api/v1/health
```

正常结果中的 `ok` 应为 `true`，`database` 应为 `configured`。

## 6. 可选服务配置

以下值必须通过服务端环境变量配置，不要写入网页代码、文档或提交到仓库：

```powershell
$env:TIANAPI_KEY = 'TianAPI 密钥'
$env:OPENCLAW_CHAT_URL = 'OpenClaw 网关地址'
$env:OPENCLAW_GATEWAY_TOKEN = 'OpenClaw 网关令牌'
```

修改环境变量后需要重启服务。

## 7. 自动化检查

自动化测试必须使用隔离测试数据库，不能连接生产数据库：

```powershell
cd D:\program\Rainbow-Cats\server
$env:DATABASE_URL = 'postgres://postgres:数据库密码@127.0.0.1:5432/rainbow_cats_manual'
npm run check
npm run test:api
npm run test:ui
npm run audit:db
```

预期结果：

- API 测试输出 `API smoke tests passed`。
- UI 测试输出 `"passed": true`。
- 数据库审计输出 `"healthy": true`。
- UI 测试结束后不会保留测试账号和业务数据。

## 8. 常见问题

### 页面无法打开

确认服务进程仍在运行，并访问健康检查地址。若端口被占用，可设置其他 `PORT`，同时修改 `WEB_ORIGIN`。

### 提示数据库未配置

确认启动服务的同一个 PowerShell 会话中已经设置 `DATABASE_URL`，并确认 PostgreSQL 服务正在运行。

### 创建空间或登录失败

确认账号未被使用、密码至少 8 位，并查看服务端终端中的错误信息。账号和昵称不是同一个字段。

### 另一位成员看不到新数据

确认双方加入的是同一个邀请码对应的空间，然后点击刷新按钮或刷新浏览器页面。

### 官方菜谱或 AI 助手不可用

这是可选服务未配置时的正常状态。分别检查 `TIANAPI_KEY`、`OPENCLAW_CHAT_URL` 和 `OPENCLAW_GATEWAY_TOKEN`。

### Windows 初始化 schema 出现编码错误

请使用当前版本的 `server/schema.sql`，并通过 `psql -v ON_ERROR_STOP=1` 执行。schema 会显式切换到 UTF-8；不要删除文件顶部的 `\encoding UTF8`。

## 9. 数据与安全

- 浏览器只访问服务端 API，不直接连接数据库。
- 生产环境必须使用 HTTPS、强随机 `SESSION_SECRET`、数据库备份和访问限流。
- 不要提交数据库密码、第三方 API key、OpenClaw token、恢复码或真实用户数据。
- 执行数据库迁移、账号删除或测试库清理前，先确认目标环境和数据库名称。
- 更完整的测试步骤和当前验证记录见 [`WEB_TEST_GUIDE.md`](WEB_TEST_GUIDE.md)。
